#import "./fragments/ImageFragment.graphql"

query GetAbout {
  page: about {
    headline
    intro
    aboutManagement
    people {
      name
      title
      email
      text
      image {
        ...ImageFragment
      }
    }
    cta
  }
}